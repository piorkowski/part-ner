<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background: #f9f9f9;
        }
        .email-content {
            background: white;
            padding: 30px;
            border-radius: 5px;
        }
        .email-header {
            border-bottom: 3px solid #667eea;
            padding-bottom: 20px;
            margin-bottom: 20px;
        }
        .email-field {
            margin-bottom: 15px;
        }
        .email-field strong {
            display: inline-block;
            width: 120px;
            color: #667eea;
        }
        .email-message {
            background: #f5f5f5;
            padding: 15px;
            border-left: 3px solid #667eea;
            margin-top: 20px;
        }
    </style>
</head>
<body>
<div class="email-container">
    <div class="email-content">
        <div class="email-header">
            <h2>Nowa wiadomość z formularza kontaktowego</h2>
            <p>Strona: Part-ner.pl</p>
        </div>

        <div class="email-field">
            <strong>Imię i nazwisko:</strong>
            {{ name }}
        </div>

        <div class="email-field">
            <strong>E-mail:</strong>
            <a href="mailto:{{ email }}">{{ email }}</a>
        </div>

        {% if phone %}
            <div class="email-field">
                <strong>Telefon:</strong>
                {{ phone }}
            </div>
        {% endif %}

        {% if company %}
            <div class="email-field">
                <strong>Firma:</strong>
                {{ company }}
            </div>
        {% endif %}

        <div class="email-field">
            <strong>Temat:</strong>
            {{ subject }}
        </div>

        <div class="email-message">
            <strong>Wiadomość:</strong><br><br>
            {{ message|nl2br }}
        </div>

        <div style="margin-top: 30px; padding-top: 20px; border-top: 1px solid #eee; font-size: 12px; color: #999;">
            <p>Język wiadomości: {{ locale|upper }}</p>
            <p>Data wysłania: {{ "now"|date("Y-m-d H:i:s") }}</p>
        </div>
    </div>
</div>
</body>
</html>
